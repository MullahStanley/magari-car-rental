# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 3D model assets (large binary GLB files)
# Excluded so Tailwind v4's content scanner doesn't try to parse them
# (scanning these multi-hundred-MB binary files hangs the compiler).
/models/
/public/models/
/supabase/models/
/supabase/.temp
